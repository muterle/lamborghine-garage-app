import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    paddingBottom: 10,
    paddingTop: 10,
    alignItems: "center",
    justifyContent: "space-around",
  },
  label: {
    color: "#FFF",
    fontSize: 22,
  },
});
